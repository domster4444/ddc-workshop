import React from 'react';
import { Button } from '@material-ui/core';
const App = () => {
  return (
    <React.Fragment>
      <Button color="primary" variant="contained">
        press me
      </Button>
    </React.Fragment>
  );
};

export default App;
