import React from 'react';
import Button from './Button';

const App = () => {
  return (
    <React.Fragment>
      <Button title="explore" subtitle="button" />
    </React.Fragment>
  );
};

export default App;
